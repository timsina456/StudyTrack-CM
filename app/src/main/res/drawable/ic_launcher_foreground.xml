<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Placeholder mark: a ledger check. Swap via Android Studio's
         Image Asset wizard (New > Image Asset) once you have real
         branding — this keeps the project buildable without binary
         image assets. -->
    <path
        android:pathData="M32,56 L48,72 L78,38"
        android:strokeColor="#EDEBE3"
        android:strokeWidth="10"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:fillColor="#00000000" />
</vector>
